import{useState}from"react";import{Sparkles,Image,PersonStanding,Boxes,Settings,Plus,SlidersHorizontal}from"lucide-react";
const nav=[["生成",Sparkles],["圖片編輯",Image],["姿勢 / 骨架",PersonStanding],["模型管理",Boxes],["設定",Settings]] as const;
export default function App(){const[active,setActive]=useState("生成");const[prompt,setPrompt]=useState("一位穿黑色外套的女性站在東京街頭，電影感光線，細節豐富");const[ratio,setRatio]=useState("1:1");const[busy,setBusy]=useState(false);const generate=()=>{setBusy(true);setTimeout(()=>setBusy(false),1200)};return <main className="app"><aside className="rail"><div className="brand"><span>✦</span><b>AI Studio</b></div><nav>{nav.map(([n,I])=><button className={active===n?"active":""} onClick={()=>setActive(n)} key={n}><I size={19}/>{n}</button>)}</nav></aside><section className="controls"><h1>{active}</h1><label>提示詞</label><textarea value={prompt} onChange={e=>setPrompt(e.target.value)}/><label>參考圖片</label><button className="drop"><Plus/>加入圖片</button><div className="row"><label>姿勢控制</label><button className="link"><PersonStanding size={16}/>開啟 Pose</button></div><label>圖片比例</label><div className="ratios">{["1:1","4:3","3:4","16:9","9:16"].map(x=><button key={x} onClick={()=>setRatio(x)} className={ratio===x?"selected":""}>{x}</button>)}</div><button className="advanced"><SlidersHorizontal size={17}/>進階設定</button><div className="stats"><span>1024 × 1024</span><span>Steps 25</span><span>Seed Random</span></div><button className="generate" onClick={generate} disabled={busy}><Sparkles size={18}/>{busy?"生成中…":"生成"}</button></section><section className="workspace"><header><div><b>Qwen Image 2.1</b><span>本機</span></div><small>GPU Engine · Ready</small></header><div className="canvas"><div className="empty"><Sparkles size={40}/><h2>{busy?"正在生成預覽…":"準備開始創作"}</h2><p>{busy?"下一階段接本機 Qwen inference。":"輸入提示詞並按下「生成」，結果會顯示在這裡。"}</p></div></div></section><aside className="history"><div><b>生成紀錄</b><button>清除</button></div><section><Image size={30}/><b>尚無生成結果</b><span>完成的圖片會保存在這裡</span></section></aside></main>}